services:
  shadowfax:
    image: ghcr.io/madeofpendletonwool/shadowfax:latest
    ports:
      - "8080:8080"
    volumes:
      - shadowfax_data:/data
    environment:
      - PORT=8080
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  shadowfax_data:
